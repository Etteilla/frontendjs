// import { createRoot } from 'react-dom/client';

// export function attachElement(elt: any, containerId:string) {
//     const containerTabs = document.getElementById(containerId);
//     containerTabs!.innerHTML = ""
//     const shadowTabs = containerTabs!.attachShadow({ mode: 'open' });
//     const rootTabs = createRoot(shadowTabs);
//     rootTabs.render());
// }